<!--index.wxml-->
<view>
    <!-- 用户登录测试 -->
    <view>
        <image  class="cover-banner" src="printer.jpeg" mode="scaleToFill"></image>
    </view>
    <view class='button'>
        <button type="primary" loading="{{userLoading}}" disable="{{userDisable}}" hover-class="button-hover" 
        data-identity="user" bindtap="onclick">我是用户</button>
    </view>
    <view class='button'>
        <button type="primary" loading="{{shopkeeperLoading}}" disable="{{shopkeeperDisable}}" hover-class="button-hover" 
        data-identity="shopkeeper" bindtap="onclick">我是商家</button>
    </view>
    <!-- 上传图片测试 -->
</view>
